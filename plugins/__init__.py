"""Plugin adapters for hindsight-memorial (currently only Claude Code).

The Hermes adapter lives at the project root: ``__init__.py`` +
``hermes_config.py``. Both adapters share the same
``hindsight_memorial.reconcile.run_reconcile`` pipeline.
"""
